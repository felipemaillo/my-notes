<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>My Notes</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  *,
  ::after,
  ::before {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --font-sans: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu,
      Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    --color-bg: #0b090b;
    --color-text: #dadada;
    --color-primary: #1d294d;
    --border-color: color-mix(in srgb, var(--color-text) 4%, var(--color-primary) 30%);
  }

  html {
    font-family: var(--font-sans);
    background-color: var(--color-bg);
    color: var(--color-text);
  }

  html.light {
    --color-bg: #dadada;
    --color-text: #0b090b;
  }

  body {
    min-height: 100vh;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-weight: 600;
  }

  p {
    line-height: 1.6;
  }
</style>
